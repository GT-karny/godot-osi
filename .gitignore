# Rust
/target
**/*.rs.bk
Cargo.lock.orig

# Temporary OSI source clone (protos already copied into proto/)
/_osi_src

# CMake build output produced inside the esmini submodule by crates/esmini-rm/build.rs
external/esmini/build*/

# Godot
.godot/
*.import

# Godot editor binary + its download archive (fetched separately, ~165MB; do not commit)
/temp/

# In-repo git worktrees for parallel feature branches (each is its own checkout)
/worktrees/

# Recorded OSI traces (test data captured at runtime; not versioned)
/traces/

# Auto-generated schema doc (emitted by crates/godot-osi/build.rs; CI bundles it)
/packaging/generated/

# OS / editor
.DS_Store
Thumbs.db
.vscode/
