# Godot ASAM OSI ラッパープラグイン 要件定義

最終更新: 2026-05-23

## 0. 目的・全体像

ASAM OSI (Open Simulation Interface) を Godot で受信・利用するための**汎用ラッパー**を作る。
特定アプリ向けの作り込みではなく、「OSI を使う Godot アプリを開発するための土台」を提供する。

構成は 2 プラグイン:

1. **OSI 受信プラグイン** — gRPC サーバーストリーミングを購読し、生 OSI メッセージを取得する。
2. **OSI 変換プラグイン** — 生 OSI を Godot で扱いやすい型付きデータへ変換する（座標変換含む）。

---

## 1. 受信対象（gRPC サービス）

`temp/` 配下の proto2 サービス定義（サーバーストリーミング）:

- `GroundTruthService.StreamGroundTruth(Empty) -> stream osi3.GroundTruth`
- `HostVehicleDataService.StreamHostVehicleData(Empty) -> stream osi3.HostVehicleData`

2 ストリームは**独立購読**を基本とする。

---

## 2. 確定した技術選定

| 項目 | 決定 |
|---|---|
| Godot バージョン | **4.x** |
| 対象プラットフォーム | **Windows デスクトップのみ** |
| 実装方式 | **Rust / godot-rust(gdext) + tonic + prost** による単一ネイティブ GDExtension |
| OSI バージョン | **v3.7.0 "Jolly Jones"**（公式 GitHub から取得して同梱。※現状未取得＝次アクション） |
| 対象データ範囲 | **GroundTruth フルセット + HostVehicleData フルセット** |
| Godot 側データ表現 | **proto 構造を保持した型付き Resource/クラス**を**ビルド時コードジェネレータで全自動生成** |
| フレーム採用方針 | **最新フレームのみ採用**（低遅延・可視化向け。受信キューは最新のみ保持し古いものは破棄） |
| オブジェクト管理 | **コア=B（型付きデータスナップショットを signal で渡す）**＋**オプション=A（id 追跡で Node3D を自動 spawn/更新/破棄するヘルパー）を同梱** |
| テスト/開発支援 | **モック gRPC サーバー + OSI トレース(.osi)再生を同梱** |

---

## 3. 座標変換（確定・既定値）

OSI（右手系・Z-up・世界座標・メートル・ラジアン / ISO 8855）→ Godot（左手系・Y-up）。

- **位置**: `Godot(x, y, z) = OSI(x, z, -y)`
- **姿勢**: OSI `roll/pitch/yaw` → Godot `Basis`/`Quaternion`（軸入れ替えに整合する符号・回転順で変換）
- **寸法**: OSI `length/width/height` → Godot `Vector3`（軸入れ替え反映）
- **単位**: メートル基準のまま（スケール係数は設定可能）
- **上記マッピングは設定で差し替え可能**にする（プロジェクト毎の軸の好みに対応）

---

## 4. プラグイン①：OSI 受信プラグイン

- tonic による gRPC クライアント。`Empty` 引数でストリーム購読開始。
- **スレッディング**: gRPC 受信はバックグラウンドスレッドで実行。Godot のノード操作はメインスレッド必須のため、**スレッド間はキュー + signal で橋渡し**。
- **接続管理**: サーバーアドレス/ポート/TLS の有無を設定可能。接続/切断/エラー/再接続を扱い、**接続状態を signal で通知**。
- **出力**: prost が生成した生 OSI メッセージ（または薄い中間表現）を変換プラグインへ渡す。
- 受信キューは**最新フレームのみ保持**（古いフレームは破棄）。

接続先（既定値）: **`127.0.0.1:50051`（TLS なし）**。アドレス/ポート/TLS は設定で外出しし上書き可能。
別途、本番用の**外部 gRPC サーバーが存在**するため、そこからトレースを録画してテストデータに用いる（§6）。

---

## 5. プラグイン②：OSI 変換プラグイン

- **コードジェネレータ**: ビルド時に osi3 .proto から Godot 用の型付きクラス（Resource 系）を全自動生成。
  - proto のフィールド名・親子関係を極力保持（フルセット）。
  - proto2 の型対応規約を定義: `required`/`optional`（nullable）/`repeated`（`Array[T]`）/`oneof`/`enum`（Godot enum or int）。
- **座標変換**は変換側で適用（§3）。
- **コア動作（B）**: 受信した最新フレームを型付き Resource のスナップショットへ変換し、**signal で丸ごと渡す**。Godot ノード（Actor 相当）には触れない。
- **オプションヘルパー（A）**: id 追跡で `MovingObject` 等を `Node3D` として自動 spawn / Transform 更新 / 消滅時 free するサンプル兼部品。コアとは分離。

---

## 6. テスト・開発支援（同梱）

- **モック gRPC サーバー**: 実サーバーなしで受信プラグインを検証できるダミーストリーム送出（既定 `127.0.0.1:50051`）。
- **OSI トレース(.osi)再生**: 記録済み OSI トレースを再生して GroundTruth/HostVehicleData ストリームを供給。
  - トレースは**既存の外部 gRPC サーバーから録画**して取得する（録画ツールも本プロジェクトで用意する想定）。
- ダミーデータ生成（簡易シーン）も検討。

---

## 7. ライセンス（商用利用：全て可）

| 依存 | ライセンス | 商用 |
|---|---|---|
| tonic | MIT | ✅ |
| prost | Apache-2.0 | ✅ |
| gdext (godot-rust) | MPL-2.0（弱コピーレフト/ファイル単位） | ✅ |
| Godot 本体 | MIT | ✅ |
| ASAM OSI .proto 定義 | MPL-2.0（弱コピーレフト/ファイル単位） | ✅ |

- **本プロジェクトの独自コード（`crates/`, `proto/service/` 等）は `MIT OR Apache-2.0` のデュアルライセンス**で配布する。
- `proto/osi3/` の ASAM OSI 定義のみ MPL-2.0 のまま据え置き（隔離ディレクトリ）。改変配布時は当該ファイルの開示義務あり。
- MPL-2.0 はファイル単位の弱コピーレフト。利用者のアプリコードは proprietary 可。
- README に各ライセンス表記と OSI 由来ファイルの出所を明記する。

---

## 8. 開発環境（確定）

- Rust: **インストール済み**（rustc/cargo 1.94.1, rustup 1.29.0、グローバル）。新規導入不要。
- 再現性のため `rust-toolchain.toml` をリポジトリに配置（バージョン/ターゲット固定）。
- ビルド成果物（`target/`）はリポジトリ内に閉じる。完全隔離が必要なら `CARGO_HOME`/`RUSTUP_HOME` をローカル化（現状は不要）。

## 9. 次アクション（実装フェーズの入口）

1. ASAM OSI **v3.7.0** の .proto 一式を公式 GitHub から取得し `proto/`（仮）へ配置。
2. Rust ワークスペース + gdext プロジェクトの雛形作成、prost/tonic ビルド設定。
3. コードジェネレータ方針の詳細設計（proto → Godot 型のマッピング規約確定）。
4. 受信プラグイン（gRPC + スレッド + signal）の骨組み。
5. 変換プラグイン（コア B）+ 座標変換。
6. モックサーバー / トレース再生。
7. オプションヘルパー A + サンプルシーン。
